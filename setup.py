from setuptools import setup, find_packages

config = {
    'name': 'climate_change',
    'description': 'Climate Change repository',
    'author': 'ste',
    'packages': find_packages()
}

setup(**config)